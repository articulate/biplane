attributes:
  uris: /content-library
  strip_uri: true
  upstream_url: http://www.example.com/public_queries
name: library_public_api
plugins:
- name: acl
  attributes:
    config:
      seacrest: {{my_val}}
      password: {{athing}}
